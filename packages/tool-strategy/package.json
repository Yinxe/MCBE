{
  "name": "@yinxe/tool-strategy",
  "version": "0.1.0",
  "private": true,
  "description": "Plug-in tool selection engine for MCBE addon monorepo (pure logic, zero @minecraft)",
  "scripts": {
    "test": "tsc -p tsconfig.test.json && node --test \".test-build/tests/**/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "5.0.2"
  }
}
